
 <div class='h-[40vh] text-[#0C2D57] flex flex-col justify-center items-center mb-10 px-5 text-center'

>
  
  <h1 class="text-4xl font-bold">Welcome to CTD Rails</h1>
  <br />
  <p class='text-2xl'>Collection of blog articles focused on Ruby on Rails, to help you learn and grow as a developer.</p>
</div>

    <div class="grid grid-cols-1 sm:grid-cols-3 md:grid-cols-4 py-6 gap-6 px-3">
      <% @posts.each do |post|%>
     <%= render partial: 'post', locals:{
          post: post
     } %>

    <% end %>
      </div>

   <% if @pagy.count > @pagy.items %>
    <%= pagy_bootstrap_nav(@pagy) %>
    <% end %>